git   is a version control system
GITHUB     
repo is considered as a folder
git clone -b branchname 
used for clone to branch

git ignore
git add .    it stored the fileon a local cache
git commit -m "enter message"    to save a file
git push -u origin main      to  main branch save
git branch -M branch name    to another branch save
git checkout -b main      to comes to main branch
git branch   which branch am currently working
git pull origin main
 
